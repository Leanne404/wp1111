import { Router } from "express";
import ScoreCard from "../models/ScoreCard";

const router = Router();

router.post("/card", ()=>{});
router.delete("/cards", ()=>{});
router.get("/cards", ()=>{});

export default router;
 